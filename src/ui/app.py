# DEPRECATED: This Streamlit frontend has been replaced by the React Vite app.
# The new React frontend is located in the `frontend/` directory.
# The backend API is located in `src/ui/api.py`.
#
# To run the backend:
#   .venv/bin/python -m uvicorn src.ui.api:app --reload --port 8000
#
# To run the frontend:
#   cd frontend && npm run dev
